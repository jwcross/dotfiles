# Use space as leader
unbind-key C-b
set -g prefix C-Space

# Mouse mode?
#set -g mouse on
#bind m \
#set -g mouse on \;\
#display 'Mouse: ON'
#bind M \
#set -g mouse on \;\
#display 'Mouse: OFF'

#set -g mouse-select-window on
#set -g mouse-select-window on \;\
#set -g mouse-select-window off \;\

# Split panes
bind-key '%' split-window -h -c "#{pane_current_path}"
bind-key '"' split-window -v -c "#{pane_current_path}"
bind-key | split-window -h -c "#{pane_current_path}"
bind-key _ split-window -v -c "#{pane_current_path}"

# Reload
bind r source-file ~/.tmux.conf

# Status bar
set -g status off

# Colors
set -g default-terminal "screen-256color"

# Vim style pane selection
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Clipboard
# set -g default-shell $SHELL
# set -g default-command "reattach-to-user-namespace -l ${SHELL}"
# unbind -T copy-mode-vi Enter
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'reattach-to-user-namespace pbcopy' # bind y to yank
# bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel 'reattach-to-user-namespace pbcopy' # bind Enter to yank
# bind-key -T copy-mode-vi v send-keys -X  begin-selection # visual selection
# bind ] run "reattach-to-user-namespace pbpaste | tmux load-buffer - && tmux paste-buffer" # bind `]` to pbpaste
